package com.example.harbour.facemeetroom.db.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.example.harbour.facemeetroom.db.entity.RoomTimes;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "ROOM_TIMES".
*/
public class RoomTimesDao extends AbstractDao<RoomTimes, Long> {

    public static final String TABLENAME = "ROOM_TIMES";

    /**
     * Properties of entity RoomTimes.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property NowNumber = new Property(1, int.class, "nowNumber", false, "NOW_NUMBER");
        public final static Property DayNumber = new Property(2, int.class, "dayNumber", false, "DAY_NUMBER");
    }


    public RoomTimesDao(DaoConfig config) {
        super(config);
    }
    
    public RoomTimesDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"ROOM_TIMES\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"NOW_NUMBER\" INTEGER NOT NULL ," + // 1: nowNumber
                "\"DAY_NUMBER\" INTEGER NOT NULL );"); // 2: dayNumber
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"ROOM_TIMES\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, RoomTimes entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getNowNumber());
        stmt.bindLong(3, entity.getDayNumber());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, RoomTimes entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getNowNumber());
        stmt.bindLong(3, entity.getDayNumber());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public RoomTimes readEntity(Cursor cursor, int offset) {
        RoomTimes entity = new RoomTimes( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getInt(offset + 1), // nowNumber
            cursor.getInt(offset + 2) // dayNumber
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, RoomTimes entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setNowNumber(cursor.getInt(offset + 1));
        entity.setDayNumber(cursor.getInt(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(RoomTimes entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(RoomTimes entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(RoomTimes entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
